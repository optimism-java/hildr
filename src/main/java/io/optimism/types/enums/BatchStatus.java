package io.optimism.types.enums;

public enum BatchStatus {
    /** Drop batch status. */
    Drop,
    /** Accept batch status. */
    Accept,
    /** Undecided batch status. */
    Undecided,
    /** Future batch status. */
    Future,
}
